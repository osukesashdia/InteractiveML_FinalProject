import type { DataStore } from '../../core/data-store';
import { Component } from '../../core/component';
export declare class Account extends Component {
    #private;
    title: string;
    constructor(dataStore: DataStore);
    mount(target?: HTMLElement): void;
}
