import { ObjectDetectionBaseModel } from '@tensorflow-models/coco-ssd';
import { type Instance, Model, ObjectDetectorResults } from '../../core';
import { Stream } from '../../core/stream';
export interface CocoSsdOptions {
    base?: ObjectDetectionBaseModel;
}
export interface CocoInstance extends Instance {
    x: ImageData;
    y: undefined;
}
export declare class CocoSsd extends Model<CocoInstance, ObjectDetectorResults> {
    #private;
    title: string;
    parameters: {};
    serviceName: string;
    $loading: Stream<boolean>;
    constructor({ base }?: CocoSsdOptions);
    setup(): Promise<void>;
    train(): never;
    predict(img: ImageData): Promise<ObjectDetectorResults>;
    mount(target?: HTMLElement): void;
    save(): never;
    load(): never;
    download(): never;
    upload(): never;
}
