import { Component } from '../../core/component';
import type { Dataset, Instance } from '../../core';
import type { ObjectId } from '../../core/types';
import { Stream } from '../../core/stream';
export interface DBInstance extends Instance {
    y: string;
}
interface DatasetBrowserOptions {
    batchSize: number;
}
export declare class DatasetBrowser extends Component {
    #private;
    title: string;
    $selected: Stream<ObjectId[]>;
    batchSize: number;
    constructor(dataset: Dataset<DBInstance>, { batchSize }?: Partial<DatasetBrowserOptions>);
    mount(target?: HTMLElement): void;
}
export {};
