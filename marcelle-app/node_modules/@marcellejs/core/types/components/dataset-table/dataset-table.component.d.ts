import { Component, Dataset, Instance, Stream } from '../../core';
export declare class DatasetTable<T extends Instance> extends Component {
    #private;
    title: string;
    $columns: Stream<string[]>;
    $selection: Stream<T[]>;
    singleSelection: boolean;
    constructor(dataset: Dataset<T>, columns?: string[], singleSelection?: boolean);
    mount(target?: HTMLElement): void;
}
