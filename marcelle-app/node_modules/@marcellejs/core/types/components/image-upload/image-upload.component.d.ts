import { Component } from '../../core/component';
import { Stream } from '../../core/stream';
export interface ImageUploadOptions {
    width?: number;
    height?: number;
}
export declare class ImageUpload extends Component {
    #private;
    title: string;
    $images: Stream<never>;
    $thumbnails: Stream<never>;
    constructor({ width, height }?: ImageUploadOptions);
    mount(target?: HTMLElement): void;
}
