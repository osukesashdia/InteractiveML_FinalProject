import { Tensor } from '@tensorflow/tfjs-core';
import { Stream } from '../../core/stream';
import { TFJSCustomClassifier, TFJSCustomClassifierOptions } from '../../core/model/tfjs-custom-classifier';
export interface MLPClassifierOptions extends TFJSCustomClassifierOptions {
    layers: number[];
}
export declare class MLPClassifier extends TFJSCustomClassifier {
    title: string;
    parameters: {
        layers: Stream<number[]>;
    } & TFJSCustomClassifier['parameters'];
    constructor({ layers, ...rest }?: Partial<MLPClassifierOptions>);
    buildModel(inputShape: Tensor['shape'], outputShape: Tensor['shape']): void;
}
