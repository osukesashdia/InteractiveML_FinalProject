import type { Parametrable } from '../../core/types';
import { Component } from '../../core/component';
type WidgetType = 'menu' | 'text' | 'boolean' | 'number' | 'number array' | 'auto';
export type ParamConfig = Record<string, {
    type: WidgetType | 'auto';
    options?: string[];
}>;
export declare class ModelParameters extends Component {
    #private;
    title: string;
    config: ParamConfig;
    constructor(m: Parametrable, config?: ParamConfig);
    mount(target?: HTMLElement): void;
}
export {};
