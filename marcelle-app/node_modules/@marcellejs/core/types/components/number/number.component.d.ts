import { Component } from '../../core/component';
import { Stream } from '../../core/stream';
export declare class Number extends Component {
    title: string;
    $value: Stream<number>;
    $disabled: Stream<boolean>;
    constructor(defaultValue?: number);
    mount(target?: HTMLElement): void;
}
