import type { Pose } from '.';
import { SupportedModels } from '@tensorflow-models/pose-detection';
export declare class SkeletonRenderer {
    model: SupportedModels;
    width: number;
    canvas: HTMLCanvasElement;
    ctx: CanvasRenderingContext2D;
    params: {
        lineWidth: number;
        radius: number;
        scoreThreshold: number;
    };
    constructor(model: SupportedModels, width: number);
    drawKeypoint(keypoint: Pose['keypoints'][0]): void;
    /**
     * Draw the keypoints on the video.
     * @param keypoints A list of keypoints.
     */
    drawKeypoints(keypoints: Pose['keypoints']): void;
    /**
     * Draw the skeleton of a body on the video.
     * @param keypoints A list of keypoints.
     */
    drawSkeleton(keypoints: Pose['keypoints'], poseId: number): void;
    drawResult(pose: Pose): void;
    drawResults(poseList: Pose[]): void;
    drawImage(img: ImageData): void;
    render(img: ImageData, poseList: Pose[], format?: string): string | ImageData;
}
