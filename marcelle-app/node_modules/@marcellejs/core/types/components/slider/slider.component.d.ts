import { Component } from '../../core/component';
import { Stream } from '../../core/stream';
export interface SliderOptions {
    values: number[];
    min: number;
    max: number;
    step: number;
    range: boolean | 'min' | 'max';
    float: boolean;
    vertical: boolean;
    pips: boolean;
    pipstep: number;
    formatter: (x: number) => unknown;
    continuous: boolean;
}
export declare class Slider extends Component {
    title: string;
    $values: Stream<number[]>;
    $min: Stream<number>;
    $max: Stream<number>;
    $step: Stream<number>;
    range: boolean | 'min' | 'max';
    float: boolean;
    vertical: boolean;
    pips: boolean;
    pipstep: number;
    formatter: (x: number) => unknown;
    continuous: boolean;
    constructor({ values, min, max, step, range, float, vertical, pips, pipstep, formatter, continuous, }?: Partial<SliderOptions>);
    mount(target?: HTMLElement): void;
}
