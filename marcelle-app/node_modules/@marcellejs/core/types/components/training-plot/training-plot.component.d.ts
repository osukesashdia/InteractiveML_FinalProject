import type { Instance, Model } from '../../core';
import { Component } from '../../core/component';
import { GenericChart } from '../generic-chart';
export type LogSpec = string | string[] | {
    [key: string]: string | string[];
};
export declare class TrainingPlot extends Component {
    model: Model<Instance, unknown>;
    title: string;
    charts: {
        [key: string]: GenericChart;
    };
    constructor(model: Model<Instance, unknown>, logs?: LogSpec);
    mount(target?: HTMLElement): void;
}
