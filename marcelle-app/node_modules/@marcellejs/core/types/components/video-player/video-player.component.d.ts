import { Component, Stream } from '../../core';
export declare class VideoPlayer extends Component {
    title: string;
    $src: Stream<string>;
    $paused: Stream<boolean>;
    $progress: Stream<number>;
    $mirror: Stream<boolean>;
    constructor(src?: string);
    mount(target?: HTMLElement): void;
}
