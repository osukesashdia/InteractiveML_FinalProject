import { Component } from '../../core/component';
import { Stream } from '../../core/stream';
export interface WebcamOptions {
    width?: number;
    height?: number;
    period?: number;
    facingMode?: 'user' | 'environment';
    audio?: boolean;
}
export declare class Webcam extends Component {
    #private;
    title: string;
    $active: Stream<boolean>;
    $ready: Stream<boolean>;
    $mediastream: Stream<MediaStream>;
    $images: Stream<ImageData>;
    $thumbnails: Stream<string>;
    $facingMode: Stream<WebcamOptions['facingMode']>;
    period: number;
    constructor({ width, height, period, facingMode, audio, }?: WebcamOptions);
    getWidth(): number;
    mount(target?: HTMLElement): void;
    stop(): void;
    setupCapture(): void;
    loadCameras(): Promise<void>;
    loadSrcStream(s: MediaStream): void;
    stopCamera(): void;
    process(): void;
    captureThumbnail(): string;
    captureImage(): ImageData;
}
