import type { DataStore } from '../data-store';
import type { Instance } from '../types';
import { Dataset } from './dataset';
export declare function dataset<T extends Instance>(name: string, store?: DataStore): Dataset<T>;
export declare function isDataset<T extends Instance>(x: unknown): x is Dataset<T>;
export type { Dataset };
