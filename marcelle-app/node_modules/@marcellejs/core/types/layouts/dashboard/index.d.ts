import { DashboardPage } from './dashboard_page';
import DashboardComponent from './Dashboard.svelte';
import { Stream } from '../../core';
import { DashboardSettings } from './dashboard_settings';
export interface DashboardOptions {
    title: string;
    author: string;
    closable?: boolean;
}
export declare class Dashboard {
    panels: Record<string, DashboardPage>;
    app?: DashboardComponent;
    settings: DashboardSettings;
    $active: Stream<boolean>;
    $page: Stream<string>;
    title: string;
    author: string;
    closable: boolean;
    constructor({ title, author, closable, }: DashboardOptions);
    page(name: string, showSidebar?: boolean): DashboardPage;
    show(): void;
    hide(): void;
}
export declare function dashboard(options: DashboardOptions): Dashboard;
