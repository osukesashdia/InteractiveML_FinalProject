import { LazyIterable } from '../../utils/lazy-iterable/lazy-iterable';
export declare class ArrayIterable<T> extends LazyIterable<T> {
    constructor(array: Array<T>);
}
export declare function iterableFromArray<T>(arr: Array<T>): ArrayIterable<T>;
